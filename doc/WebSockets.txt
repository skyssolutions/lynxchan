The ports used for ws and wss are informed in every thread's json. 
After connecting, send a string in the format boardUri-threadId to be informed every time a reply is made on that thread. A string with the id of the new post will be sent through the websocket.
